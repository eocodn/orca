import { detectAgentStatusFromTitle, type AgentStatus } from '../../../../shared/agent-detection'; import type { ParsedAgentStatusPayload } from '../../../../shared/agent-status-types'; import {   isRecognizedAgentType,   recognizeAgentProcess,   type RecognizedAgentProcess } from '../../../../shared/agent-process-recognition'; import {   enqueueAgentProcessInspection,   type InspectionPriority } from './agent-process-inspection-queue'; import type {   AgentCompletionCoordinator,   AgentCompletionCoordinatorOptions,   AgentCompletionStatusSnapshot } from './agent-completion-coordinator-types'; import type { RuntimeTerminalProcessInspection } from '@/runtime/runtime-terminal-inspection'; import { isPiCompatibleAgentType } from '../../../../shared/pi-agent-kind'; import {   titleHasExplicitAgentIdentity,   titleIsInconclusiveNativeDroidTitle } from './title-agent-identity'; type CompletionSource = 'hook' | 'title' | 'process-exit'; type CompletionIdentitySource = 'hook' | 'title' | 'process-exit'; type PollCadenceTier = 'active' | 'idle' | 'hidden' | 'no-evidence'; type LastCompletionIdentity = {   source: CompletionIdentitySource   identity: string   agentIdentity: string | null }; const lastCompletionIdentityByPaneKey = new Map<string, LastCompletionIdentity>(); const IDLE_POLL_INTERVAL_MS = 2_000; const ACTIVE_POLL_INTERVAL_MS = 750; const HIDDEN_POLL_INTERVAL_MS = 3_000; const NO_EVIDENCE_POLL_INTERVAL_MS = 15_000; const NO_EVIDENCE_ACTIVITY_HOT_WINDOW_MS = 10_000; const INSPECTION_TIMEOUT_MS = 15_000; const PENDING_TITLE_TTL_MS = Math.max(2_000, INSPECTION_TIMEOUT_MS + 500); const PENDING_TITLE_MAX_TTL_MS = Math.max(30_000, PENDING_TITLE_TTL_MS); const COMPLETION_REPLAY_GUARD_MS = 1_000; const HOOK_DONE_QUIET_MS = 1_500; const CODEX_ATTENTION_QUIET_MS = 1_500; const POLL_TIER_INTERVAL_MS: Record<PollCadenceTier, number> = {   active: ACTIVE_POLL_INTERVAL_MS,   idle: IDLE_POLL_INTERVAL_MS,   hidden: HIDDEN_POLL_INTERVAL_MS,   'no-evidence': NO_EVIDENCE_POLL_INTERVAL_MS }; function isCompletionHookState(state: ParsedAgentStatusPayload['state']): boolean {   return state === 'done' }; function isAttentionHookState(state: ParsedAgentStatusPayload['state']): boolean {   return state === 'waiting' || state === 'blocked' }; export function createAgentCompletionCoordinator(   options: AgentCompletionCoordinatorOptions ): AgentCompletionCoordinator {   let disposed = false;   let agentIdentityEstablished = false;   let hasAgentRunEvidence = false;   let workingStatusObserved = false;   let lastTitleStatus: AgentStatus | null = null;   let currentTurn = 0;   let processSession = 0;   let lastCompletionToken: string | null = null;   let lastCompletionAt = 0;   let lastCompletedTurn: number | null = null;   let lastCompletionSource: CompletionSource | null = null;   let lastCompletionIdentity: LastCompletionIdentity | null = null;   let lastAttentionToken: string | null = null;   let lastForegroundAgent: RecognizedAgentProcess | null = null;   let requiresFreshWorking = false;   let pollTimer: ReturnType<typeof setTimeout> | null = null;   let pendingTitleTimer: ReturnType<typeof setTimeout> | null = null;   let pendingHookDoneTimer: ReturnType<typeof setTimeout> | null = null;   let pendingHookDoneTitle: string | null = null;   let pendingHookDonePayload: AgentCompletionStatusSnapshot | null = null;   let pendingCodexAttentionTimer: ReturnType<typeof setTimeout> | null = null;   let pendingProcessExitAgent: RecognizedAgentProcess | null = null;   let pendingTitleSequence = 0;   let pendingTitle: {     id: number     title: string     expiresAt: number     maxExpiresAt: number     firstInspectionFinished: boolean     validatedByFreshInspection: boolean   } | null = null;   let inspectionInFlight = false;   let inspectionGeneration = 0;   let consecutiveInspectionErrors = 0;   let pollTrackingStarted = false;   let pollTimerTier: PollCadenceTier | null = null;   let lastPaneActivityAt = 0;   function clearPollTimer(): void {     if (pollTimer === null) {       return     }     clearTimeout(pollTimer)     pollTimer = null     pollTimerTier = null   };   function clearPendingTitleTimer(): void {     if (pendingTitleTimer === null) {       return     }     clearTimeout(pendingTitleTimer)     pendingTitleTimer = null   };   function clearPendingHookDone(): void {     if (pendingHookDoneTimer !== null) {       clearTimeout(pendingHookDoneTimer)       pendingHookDoneTimer = null     }     pendingHookDoneTitle = null     pendingHookDonePayload = null   };   function clearPendingCodexAttention(): void {     if (pendingCodexAttentionTimer !== null) {       clearTimeout(pendingCodexAttentionTimer)       pendingCodexAttentionTimer = null     }   };   function establishAgentEvidence(): void {     agentIdentityEstablished = true     hasAgentRunEvidence = true     scheduleNextPoll()   };   function clearAgentRunEvidence(): void {     agentIdentityEstablished = false     hasAgentRunEvidence = false     workingStatusObserved = false     pendingProcessExitAgent = null     dropPendingTitle()   };   function completionToken(source: CompletionSource): string {     if (workingStatusObserved) {       return `turn:${currentTurn}`     };     if (lastForegroundAgent) {       return `process:${processSession}`     };     return `${source}:${currentTurn}:${processSession}`   };   function hookCompletionIdentity(payload: AgentCompletionStatusSnapshot): string | null {     if (typeof payload.stateStartedAt !== 'number' || !Number.isFinite(payload.stateStartedAt)) {       return null     };     return [       payload.state,       payload.agentType ?? '',       String(Math.trunc(payload.stateStartedAt))     ].join(':')   };   function hookCompletionAgentIdentity(payload: AgentCompletionStatusSnapshot): string | null {     return payload.agentType?.trim().toLowerCase() || null   };   function doneShouldUseQuietWindow(payload: AgentCompletionStatusSnapshot): boolean {     return workingStatusObserved || isPiCompatibleAgentType(hookCompletionAgentIdentity(payload))   };   function hookAttentionToken(payload: AgentCompletionStatusSnapshot): string {     const identity = hookCompletionIdentity(payload);     if (identity) {       return `identity:${identity}`     };     return [       'turn',       String(currentTurn),       payload.state,       payload.agentType ?? '',       payload.toolName ?? '',       payload.toolInput ?? '',       payload.prompt     ].join(':')   };   function titleCompletionIdentity(title: string): string {     return title   };   function titleCompletionAgentIdentity(title: string): string | null {     const normalized = title.toLowerCase();     if (/\bcodex\b/.test(normalized)) {       return 'codex'     };     if (/\bclaude\b/.test(normalized)) {       return 'claude'     };     if (/\bgemini\b/.test(normalized)) {       return 'gemini'     };     if (/\bcursor(?: agent)?\b/.test(normalized)) {       return 'cursor'     };     if (/\bopencode\b/.test(normalized)) {       return 'opencode'     };     if (/\bdroid\b/.test(normalized)) {       return 'droid'     };     if (/\bhermes\b/.test(normalized)) {       return 'hermes'     };     if (/\baider\b/.test(normalized)) {       return 'aider'     };     if (/\bpi\b/.test(normalized) || normalized.includes('\u03c0')) {       return 'pi'     };     return null   };   function completionIdentityAlreadyNotified(     completionIdentity: LastCompletionIdentity | null | undefined   ): boolean {     if (!completionIdentity) {       return false     };     const previous = lastCompletionIdentityByPaneKey.get(options.paneKey);     if (!previous) {       return false     };     if (previous.source === completionIdentity.source) {       return previous.identity === completionIdentity.identity     };     return (       previous.agentIdentity !== null &&       completionIdentity.agentIdentity !== null &&       previous.agentIdentity === completionIdentity.agentIdentity     )   };   function dispatchCompletion(     source: CompletionSource,     title: string,     optionsOverride: {       quietedHookDone?: boolean       terminalIdleConfirmed?: boolean       agentStatus?: AgentCompletionStatusSnapshot       completionIdentity?: LastCompletionIdentity | null     } = {}   ): void {     if (source !== 'hook' && pendingHookDoneTimer !== null) {       return     };     if (requiresFreshWorking || lastCompletedTurn === currentTurn) {       return     };     if (!options.isLive() || !hasAgentRunEvidence) {       return     };     const now = Date.now();     const token = completionToken(source);     if (token === lastCompletionToken && now - lastCompletionAt < COMPLETION_REPLAY_GUARD_MS) {       return     };     if (completionIdentityAlreadyNotified(optionsOverride.completionIdentity)) {       return     }     lastCompletionToken = token     lastCompletionAt = now     lastCompletedTurn = currentTurn     lastCompletionSource = source     workingStatusObserved = false     clearPendingCodexAttention();     if (optionsOverride.completionIdentity) {       lastCompletionIdentityByPaneKey.set(options.paneKey, optionsOverride.completionIdentity)     };     if (source === 'hook' && optionsOverride.agentStatus) {       options.dispatchHookLifecycle?.(optionsOverride.agentStatus)     };     if (optionsOverride.quietedHookDone === true || source === 'process-exit') {       options.dispatchCompletion(title, {         source,         quietedHookDone: optionsOverride.quietedHookDone === true,         ...(optionsOverride.terminalIdleConfirmed === true ? { terminalIdleConfirmed: true } : {}),         ...(optionsOverride.agentStatus ? { agentStatus: optionsOverride.agentStatus } : {})       })     } else {       options.dispatchCompletion(title)     }   };   function dispatchAttentionNotification(payload: AgentCompletionStatusSnapshot): void {     options.dispatchAttention?.(payload.agentType ?? options.paneKey, {       source: 'hook',       agentStatus: payload     })   };   function dispatchAttention(payload: AgentCompletionStatusSnapshot): void {     if (!options.dispatchAttention || !options.isLive() || !hasAgentRunEvidence) {       return     };     const token = hookAttentionToken(payload);     if (token === lastAttentionToken) {       return     }     lastAttentionToken = token     options.dispatchHookLifecycle?.(payload);     if (payload.agentType === 'codex') {       clearPendingCodexAttention()       pendingCodexAttentionTimer = setTimeout(() => {         pendingCodexAttentionTimer = null;         if (!options.isLive() || !hasAgentRunEvidence) {           return         }         dispatchAttentionNotification(payload)       }, CODEX_ATTENTION_QUIET_MS);       return     }     dispatchAttentionNotification(payload)   };   function scheduleHookDoneCompletion(title: string, payload: AgentCompletionStatusSnapshot): void {     pendingHookDoneTitle = title     pendingHookDonePayload = payload;     if (pendingHookDoneTimer !== null) {       return     }     pendingHookDoneTimer = setTimeout(() => {       pendingHookDoneTimer = null;       const pendingTitle = pendingHookDoneTitle;       const pendingPayload = pendingHookDonePayload       pendingHookDoneTitle = null       pendingHookDonePayload = null;       if (pendingTitle) {         const hookIdentity = pendingPayload ? hookCompletionIdentity(pendingPayload) : null         dispatchCompletion('hook', pendingTitle, {           quietedHookDone: true,           ...(pendingPayload ? { agentStatus: pendingPayload } : {}),           ...(hookIdentity             ? {                 completionIdentity: {                   source: 'hook',                   identity: hookIdentity,                   agentIdentity: pendingPayload ? hookCompletionAgentIdentity(pendingPayload) : null                 }               }             : {})         })       }     }, HOOK_DONE_QUIET_MS)   };   function dropPendingTitle(): void {     clearPendingTitleTimer()     pendingTitle = null   };   function dispatchPendingTitleIfEligible(): void {     if (       !pendingTitle ||       !pendingTitle.validatedByFreshInspection ||       !agentIdentityEstablished ||       !hasAgentRunEvidence     ) {       return     };     const title = pendingTitle.title     dropPendingTitle()     markTitleCompletionNotified(title)     dispatchCompletion('title', title, {       completionIdentity: {         source: 'title',         identity: titleCompletionIdentity(title),         agentIdentity: titleCompletionAgentIdentity(title)       }     })   };   function schedulePendingTitleExpiry(): void {     clearPendingTitleTimer();     const pending = pendingTitle;     if (!pending) {       return     };     const remaining = pending.expiresAt - Date.now();     if (remaining <= 0) {       pendingTitle = null       scheduleNextPoll();       return     }     pendingTitleTimer = setTimeout(() => {       pendingTitleTimer = null;       if (!pendingTitle) {         return       };       if (!pendingTitle.firstInspectionFinished && Date.now() < pendingTitle.maxExpiresAt) {         pendingTitle.expiresAt = Math.min(Date.now() + 500, pendingTitle.maxExpiresAt)         schedulePendingTitleExpiry();         return       }       pendingTitle = null       scheduleNextPoll()     }, remaining)   };   function holdTitleCompletionPending(title: string): void {     const now = Date.now()     pendingTitle = {       id: ++pendingTitleSequence,       title,       expiresAt: Math.min(now + PENDING_TITLE_TTL_MS, now + PENDING_TITLE_MAX_TTL_MS),       maxExpiresAt: now + PENDING_TITLE_MAX_TTL_MS,       firstInspectionFinished: false,       validatedByFreshInspection: false     }     schedulePendingTitleExpiry()     requestInspection('pending-title')   };   function handleRecognizedProcess(process: RecognizedAgentProcess): void {     pendingProcessExitAgent = null;     if (lastForegroundAgent?.agent !== process.agent) {       if (lastForegroundAgent && hasAgentRunEvidence) {         if (           options.shouldSuppressProcessReplacementCompletion?.(lastForegroundAgent, process) !==           true         ) {           dispatchCompletion('process-exit', lastForegroundAgent.processName, {             completionIdentity: {               source: 'process-exit',               identity: `${lastForegroundAgent.agent}:${lastForegroundAgent.processName}`,               agentIdentity: lastForegroundAgent.agent             }           })         }       }       processSession += 1     }     lastForegroundAgent = process     establishAgentEvidence()   };   function handleProcessInspectionResult(result: RuntimeTerminalProcessInspection): boolean {     if (result.unavailable === true) {       pendingProcessExitAgent = null       consecutiveInspectionErrors += 1       scheduleNextPoll();       return false     }     consecutiveInspectionErrors = 0;     const recognized = recognizeAgentProcess(result.foregroundProcess);     if (recognized) {       handleRecognizedProcess(recognized);       return true     };     if (pendingHookDoneTimer !== null || pendingCodexAttentionTimer !== null) {       scheduleNextPoll();       return false     };     if (lastForegroundAgent && hasAgentRunEvidence) {       if (result.hasChildProcesses) {         pendingProcessExitAgent = null         scheduleNextPoll();         return false       };       if (         !pendingProcessExitAgent ||         pendingProcessExitAgent.agent !== lastForegroundAgent.agent ||         pendingProcessExitAgent.processName !== lastForegroundAgent.processName       ) {         pendingProcessExitAgent = lastForegroundAgent         scheduleNextPoll();         return false       };       const exited = lastForegroundAgent       pendingProcessExitAgent = null;       if (options.shouldSuppressConfirmedProcessExitCompletion?.(exited) !== true) {         dispatchCompletion('process-exit', exited.processName, {           terminalIdleConfirmed: true,           completionIdentity: {             source: 'process-exit',             identity: `${exited.agent}:${exited.processName}`,             agentIdentity: exited.agent           }         })       }       lastForegroundAgent = null       clearAgentRunEvidence()     } else {       lastForegroundAgent = null       clearAgentRunEvidence()     };     return false   };   function requestInspection(priority: InspectionPriority): void {     if (disposed || inspectionInFlight || !options.isLive()) {       return     };     if (priority === 'cadence' && !shouldRunCadenceInspection()) {       return     };     const ptyId = options.getPtyId();     if (!ptyId) {       return     }     inspectionInFlight = true;     const generationAtRequest = inspectionGeneration;     const pendingTitleIdAtRequest = priority === 'pending-title' ? pendingTitle?.id : null     enqueueAgentProcessInspection({       priority,       run: async () => {         let inspectedRecognizedAgent = false;         let inspectionSucceeded = false;         try {           const result = await options.inspectProcess(options.getSettings(), ptyId);           if (!disposed && generationAtRequest === inspectionGeneration) {             const appliesToCurrentPendingTitle =               !pendingTitle ||               (priority === 'pending-title' && pendingTitle.id === pendingTitleIdAtRequest);             if (appliesToCurrentPendingTitle) {               inspectedRecognizedAgent = handleProcessInspectionResult(result)             }             inspectionSucceeded = true           }         } catch {           pendingProcessExitAgent = null           consecutiveInspectionErrors += 1         } finally {           inspectionInFlight = false;           if (generationAtRequest !== inspectionGeneration) {             if (pendingTitle) {               requestInspection('pending-title')             } else {               scheduleNextPoll()             }           } else {             if (pendingTitle) {               if (priority === 'pending-title' && pendingTitle.id === pendingTitleIdAtRequest) {                 pendingTitle.firstInspectionFinished = true;                 if (inspectionSucceeded && inspectedRecognizedAgent) {                   pendingTitle.validatedByFreshInspection = true                   dispatchPendingTitleIfEligible()                 } else if (!inspectionSucceeded) {                   dropPendingTitle()                 }                 schedulePendingTitleExpiry()               } else {                 requestInspection('pending-title')               }             }             scheduleNextPoll()           }         }       }     })   };   function shouldRunCadenceInspection(): boolean {     return (       hasAgentRunEvidence ||       lastForegroundAgent !== null ||       options.shouldPollProcessCadence?.() !== false     )   };   function isHiddenBackstop(): boolean {     return options.shouldPollProcessCadence?.() === false   };   function paneActivityWithinHotWindow(): boolean {     return (       lastPaneActivityAt > 0 && Date.now() - lastPaneActivityAt < NO_EVIDENCE_ACTIVITY_HOT_WINDOW_MS     )   };   function currentPollTier(): PollCadenceTier {     if (isHiddenBackstop()) {       return 'hidden'     };     if (lastForegroundAgent) {       return 'active'     };     if (hasAgentRunEvidence) {       return 'idle'     };     if (options.isProcessInspectionCostly?.() === true && !paneActivityWithinHotWindow()) {       return 'no-evidence'     };     return 'idle'   };   function nextPollInterval(tier: PollCadenceTier): number {     const base = POLL_TIER_INTERVAL_MS[tier];     const backoff =       consecutiveInspectionErrors > 0         ?           Math.min(Math.max(10_000, base), base * 2 ** consecutiveInspectionErrors)         : base;     const jitter = 1 + (Math.random() * 0.2 - 0.1);     return Math.round(backoff * jitter)   };   function scheduleNextPoll(): void {     if (disposed || !pollTrackingStarted || !options.isLive() || pendingTitle) {       return     };     const tier = currentPollTier();     if (pollTimer !== null) {       if (         pollTimerTier !== null &&         POLL_TIER_INTERVAL_MS[tier] < POLL_TIER_INTERVAL_MS[pollTimerTier]       ) {         clearPollTimer()       } else {         return       }     };     if (!shouldRunCadenceInspection()) {       return     };     const ptyId = options.getPtyId();     if (!ptyId) {       return     }     pollTimerTier = tier     pollTimer = setTimeout(() => {       pollTimer = null       pollTimerTier = null       requestInspection('cadence')     }, nextPollInterval(tier))   };   function recordPaneActivity(): void {     lastPaneActivityAt = Date.now();     if (pollTimer === null || pollTimerTier === 'no-evidence') {       scheduleNextPoll()     }   };   function observeOutputActivity(): void {     recordPaneActivity()   };   function recordTitleWorking(): boolean {     clearPendingHookDone();     if (       lastCompletionSource === 'hook' &&       Date.now() - lastCompletionAt < COMPLETION_REPLAY_GUARD_MS     ) {       return false     }     clearPendingCodexAttention()     workingStatusObserved = true     requiresFreshWorking = false     lastCompletionIdentityByPaneKey.delete(options.paneKey)     currentTurn += 1     dropPendingTitle();     return true   };   function observeTitleWorking(): void {     recordTitleWorking()   };   function observeTitle(title: string): void {     recordPaneActivity();     const status = detectAgentStatusFromTitle(title);     const isInconclusiveNativeDroidTitle = titleIsInconclusiveNativeDroidTitle(title);     const hasExplicitAgentIdentity =       titleHasExplicitAgentIdentity(title) && !isInconclusiveNativeDroidTitle;     const hadPendingTitle = pendingTitle !== null;     if (hasExplicitAgentIdentity) {       establishAgentEvidence()     };     if (status === 'working') {       if (!recordTitleWorking()) {         return       }     } else if (lastTitleStatus === 'working') {       if (isInconclusiveNativeDroidTitle) {         lastTitleStatus = status;         return       };       if (status === null && !titleHasExplicitAgentIdentity(title)) {         holdTitleCompletionPending(title)         lastTitleStatus = status;         return       };       if (agentIdentityEstablished && hasAgentRunEvidence) {         markTitleCompletionNotified(title)         dispatchCompletion('title', title, {           completionIdentity: {             source: 'title',             identity: titleCompletionIdentity(title),             agentIdentity: titleCompletionAgentIdentity(title)           }         })       } else {         holdTitleCompletionPending(title)       }     } else if (hadPendingTitle && status !== null && hasExplicitAgentIdentity) {       dropPendingTitle()       markTitleCompletionNotified(title)       dispatchCompletion('title', title, {         completionIdentity: {           source: 'title',           identity: titleCompletionIdentity(title),           agentIdentity: titleCompletionAgentIdentity(title)         }       })     }     lastTitleStatus = status   };   function observeClassifiedTitleCompletion(title: string): void {     if (titleHasExplicitAgentIdentity(title)) {       establishAgentEvidence()     };     if (agentIdentityEstablished && hasAgentRunEvidence) {       markTitleCompletionNotified(title)       dispatchCompletion('title', title, {         completionIdentity: {           source: 'title',           identity: titleCompletionIdentity(title),           agentIdentity: titleCompletionAgentIdentity(title)         }       })     } else {       holdTitleCompletionPending(title)     }   };   function observeHookStatus(payload: AgentCompletionStatusSnapshot): void {     recordPaneActivity();     if (options.shouldSuppressHookCompletion?.(payload)) {       if (isAttentionHookState(payload.state)) {         clearPendingHookDone()         clearPendingCodexAttention()       };       return     };     if (isRecognizedAgentType(payload.agentType)) {       establishAgentEvidence()     };     if (payload.state === 'working') {       clearPendingHookDone()       clearPendingCodexAttention()       workingStatusObserved = true       requiresFreshWorking = false       lastCompletionIdentity = null       lastAttentionToken = null       currentTurn += 1       dropPendingTitle()       options.dispatchHookLifecycle?.(payload);       return     };     if (isAttentionHookState(payload.state)) {       clearPendingHookDone()       dispatchAttention(payload);       return     };     if (isCompletionHookState(payload.state)) {       clearPendingCodexAttention();       if (isRecognizedAgentType(payload.agentType)) {         establishAgentEvidence()       };       const hookIdentity = hookCompletionIdentity(payload);       if (         hookIdentity &&         lastCompletionIdentity?.source === 'hook' &&         hookIdentity === lastCompletionIdentity.identity       ) {         if (payload.state === 'done' && pendingHookDoneTimer !== null) {           scheduleHookDoneCompletion(payload.agentType ?? options.paneKey, payload)         };         return       };       if (         !workingStatusObserved &&         lastCompletionSource === 'hook' &&         lastCompletedTurn === currentTurn &&         Date.now() - lastCompletionAt >= COMPLETION_REPLAY_GUARD_MS       ) {         currentTurn += 1       };       if (payload.state === 'done' && doneShouldUseQuietWindow(payload)) {         lastCompletionIdentity = hookIdentity           ? {               source: 'hook',               identity: hookIdentity,               agentIdentity: hookCompletionAgentIdentity(payload)             }           : null         scheduleHookDoneCompletion(payload.agentType ?? options.paneKey, payload);         return       }       lastCompletionIdentity = hookIdentity         ? {             source: 'hook',             identity: hookIdentity,             agentIdentity: hookCompletionAgentIdentity(payload)           }         : null       dispatchCompletion('hook', payload.agentType ?? options.paneKey, {         agentStatus: payload,         ...(lastCompletionIdentity ? { completionIdentity: lastCompletionIdentity } : {})       })     }   };   function markTitleCompletionNotified(title: string): void {     lastCompletionIdentity = {       source: 'title',       identity: titleCompletionIdentity(title),       agentIdentity: titleCompletionAgentIdentity(title)     }   };   function startProcessTracking(): void {     pollTrackingStarted = true     scheduleNextPoll()   };   function hasPendingHookDoneCompletion(): boolean {     return pendingHookDoneTimer !== null   };   function resetCompletionState(options: { requireFreshWorking?: boolean } = {}): void {     clearPendingHookDone()     clearPendingCodexAttention()     dropPendingTitle()     agentIdentityEstablished = false     hasAgentRunEvidence = false     workingStatusObserved = false     lastTitleStatus = null     lastCompletionToken = null     lastCompletionAt = 0     lastCompletedTurn = null     lastCompletionSource = null     lastCompletionIdentity = null     lastAttentionToken = null     lastForegroundAgent = null     requiresFreshWorking = options.requireFreshWorking ?? false     inspectionGeneration += 1   };   function dispose(): void {     disposed = true     clearPollTimer()     clearPendingHookDone()     clearPendingCodexAttention()     dropPendingTitle();     if (!options.isLive()) {       lastCompletionIdentityByPaneKey.delete(options.paneKey)     }   };   return {     observeTitle,     observeClassifiedTitleCompletion,     observeTitleWorking,     observeOutputActivity,     observeHookStatus,     startProcessTracking,     hasPendingHookDoneCompletion,     resetCompletionState,     dispose   } }; export function resetAgentCompletionCoordinatorIdentitiesForTest(): void {   lastCompletionIdentityByPaneKey.clear() }; export function getAgentCompletionCoordinatorIdentityCountForTest(): number {   return lastCompletionIdentityByPaneKey.size }
